# Google MCP configuration
# Default services are gmail, calendar, drive, docs, sheets, slides.

# Comma/space-separated list, or "all" for every service.
# GOOGLE_MCP_SERVICES=gmail,calendar,drive,docs,sheets,slides

# Scope profile: readonly, editor, full.
# GOOGLE_MCP_SCOPE_PROFILE=readonly

# Optional explicit scopes override (comma/space-separated).
# GOOGLE_MCP_SCOPES=

# Gmail permissions (defaults depend on profile).
# GOOGLE_MCP_GMAIL_COMPOSE=true
# GOOGLE_MCP_GMAIL_MODIFY=false
# GOOGLE_MCP_GMAIL_SEND=false

# Calendar write permissions (create/update/delete).
# GOOGLE_MCP_CALENDAR_WRITE=false
